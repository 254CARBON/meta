<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ metadata.title }} - 254Carbon Platform Overview</title>
    <link rel="stylesheet" href="assets/dashboard.css">
    <script src="https://cdn.plot.ly/plotly-latest.min.js"></script>
</head>
<body>
    <div class="dashboard-header">
        <h1>🏗️ {{ metadata.title }}</h1>
        <p class="timestamp">Generated: {{ metadata.generated_at }}</p>
        <p class="version">Version: {{ metadata.version }}</p>
    </div>

    <div class="metrics-grid">
        <div class="metric-card">
            <div class="metric-label">Total Services</div>
            <div class="metric-value">{{ platform_summary.total_services }}</div>
        </div>

        <div class="metric-card">
            <div class="metric-label">Platform Health</div>
            <div class="metric-value {% if quality_overview.average_score >= 80 %}status-good{% elif quality_overview.average_score >= 70 %}status-warning{% else %}status-danger{% endif %}">
                {{ quality_overview.average_score|round(1) }}/100
            </div>
        </div>

        <div class="metric-card">
            <div class="metric-label">Drift Issues</div>
            <div class="metric-value {% if drift_overview.total_issues == 0 %}status-good{% elif drift_overview.total_issues <= 5 %}status-warning{% else %}status-danger{% endif %}">
                {{ drift_overview.total_issues }}
            </div>
        </div>

        <div class="metric-card">
            <div class="metric-label">Domains</div>
            <div class="metric-value">{{ platform_summary.total_domains }}</div>
        </div>
    </div>

    <div class="chart-container">
        <h2>📊 Quality Score Distribution</h2>
        {{ charts.quality_distribution|safe }}
    </div>

    <div class="chart-container">
        <h2>🏗️ Services by Domain</h2>
        {{ charts.domain_distribution|safe }}
    </div>

    <div class="chart-container">
        <h2>🔍 Drift Issues by Severity</h2>
        {{ charts.drift_issues|safe }}
    </div>

    <div class="footer">
        <p>🤖 Generated by 254Carbon Meta Repository</p>
        <p><a href="https://github.com/254carbon/254carbon-meta">View Source</a></p>
    </div>

    <script src="assets/dashboard.js"></script>
</body>
</html>
