<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ metadata.title }} - {{ team_summary.domain }} Team</title>
    <link rel="stylesheet" href="assets/dashboard.css">
    <script src="https://cdn.plot.ly/plotly-latest.min.js"></script>
</head>
<body>
    <div class="dashboard-header">
        <h1>👥 {{ team_summary.domain|title }} Team Dashboard</h1>
        <p class="timestamp">Generated: {{ metadata.generated_at }}</p>
    </div>

    <div class="metrics-grid">
        <div class="metric-card">
            <div class="metric-label">Team Services</div>
            <div class="metric-value">{{ team_summary.total_services }}</div>
        </div>

        <div class="metric-card">
            <div class="metric-label">Average Quality</div>
            <div class="metric-value {% if team_summary.average_quality >= 80 %}status-good{% elif team_summary.average_quality >= 70 %}status-warning{% else %}status-danger{% endif %}">
                {{ team_summary.average_quality|round(1) }}
            </div>
        </div>

        <div class="metric-card">
            <div class="metric-label">Maturity Levels</div>
            <div class="metric-value">{{ team_summary.maturity_distribution|length }}</div>
        </div>
    </div>

    <div class="chart-container">
        <h2>📊 Quality Distribution</h2>
        {{ charts.quality_distribution|safe }}
    </div>

    <div class="services-section">
        <h2>🔧 Team Services</h2>
        <table>
            <thead>
                <tr>
                    <th>Service</th>
                    <th>Maturity</th>
                    <th>Version</th>
                    <th>Quality Score</th>
                    <th>Grade</th>
                    <th>Dependencies</th>
                </tr>
            </thead>
            <tbody>
            {% for service in services %}
                <tr>
                    <td><a href="service-{{ service.name }}.html">{{ service.name }}</a></td>
                    <td>{{ service.maturity|title }}</td>
                    <td>{{ service.version }}</td>
                    <td class="{% if service.quality_score >= 80 %}status-good{% elif service.quality_score >= 70 %}status-warning{% else %}status-danger{% endif %}">
                        {{ service.quality_score|round(1) }}
                    </td>
                    <td>{{ service.quality_grade }}</td>
                    <td>{{ service.dependencies.internal|length }} internal, {{ service.dependencies.external|length }} external</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="footer">
        <p>🤖 Generated by 254Carbon Meta Repository</p>
        <p><a href="index.html">Back to Platform Overview</a></p>
    </div>

    <script src="assets/dashboard.js"></script>
</body>
</html>
